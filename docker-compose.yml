services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ngit-app
    restart: always
    ports:
      - "3002:3000"
    env_file:
      - .env.production
    # volumes:
    #   - ./public/uploads:/app/public/uploads # Only if using local file storage
